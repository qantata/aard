{
  "name": "aard",
  "version": "0.0.0-development",
  "private": true,
  "license": "AGPL-3.0",
  "scripts": {
    "delete-deps": "rm -rf apps/*/node_modules && rm -rf packages/*/node_modules"
  },
  "devDependencies": {
    "@semantic-release/commit-analyzer": "^9.0.1",
    "@semantic-release/github": "^8.0.2",
    "semantic-release": "^18.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
